%define package @PACKAGE@
%define version @VERSION@
%define release 1

Summary:     OpenCOBOL - COBOL compiler.
Name:        %{package}
Version:     %{version}
Release:     %{release}
Source:      http://prdownloads.sourceforge.net/open-cobol/%{package}-%{version}.tar.gz
Group:       Development/Languages
Copyright:   GPL, LGPL
Packager:    Fuhito Suguri <bitwalk@jcom.home.ne.jp>
Vendor:      bitWalk Co., Ltd.
Buildroot:   %{_tmppath}/%{package}-%{version}-buildroot

%description
OpenCOBOL is an open-source COBOL compiler, translating COBOL to C.

Related information can be obtained in the following site:

  http://open-cobol.sourceforge.net/

%description -l ja
OpenCOBOL はオープンソースな COBOL コンパイラです。
COBOL で書かれたプログラムを C 言語に変換して実行します。

OpenCOBOL に関する情報は、以下のサイトから得られます。

  http://open-cobol.sourceforge.net/

%prep
rm -rf $RPM_BUILD_ROOT

%setup

%build
%configure
make

%install
%makeinstall

%files
%defattr (-,root,root)
%doc AUTHORS COPYING COPYING.LIB ChangeLog INSTALL NEWS README THANKS VERSION
%{_bindir}
%{_libdir}
%{_includedir}
%{_infodir}/open-cobol.info*
%{_datadir}/aclocal/*

%post
/sbin/ldconfig

%postun
/sbin/ldconfig

%changelog
